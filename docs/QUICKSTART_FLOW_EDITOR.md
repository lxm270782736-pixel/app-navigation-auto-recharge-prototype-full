# 快速开始 - 可视化任务流程编辑器

## 5 分钟上手指南

### 1. 启动项目

```bash
# 终端 1: 启动 mock ROS 服务器
python3 mock_rosbridge.py

# 终端 2: 启动前端
npm run preview

# 浏览器访问
http://localhost:4173
```

### 2. 进入任务配置

1. 点击左侧菜单进入 **Navigation** 页面
2. 点击右侧面板的 **"配置任务"** 按钮
3. 在弹出的模态框顶部,点击 **"流程图模式"**

### 3. 创建第一个任务流程

#### 场景: 机器人到达目标点后拍照并播报

**步骤**:

1. **拖拽任务节点**
   - 从左侧工具箱拖拽 📷 **"拍照"** 到画布中间
   - 拖拽 🔊 **"播放声音"** 到拍照节点下方

2. **连接任务**
   - 点击 🚀 **"开始"** 节点底部的圆点
   - 拖动到 📷 **"拍照"** 节点顶部的圆点
   - 释放鼠标,连接线创建成功
   - 同样方式连接: 拍照 → 播放声音 → 结束

3. **配置参数**
   - 点击"拍照"节点
   - 修改"拍照数量"为 **3 张**
   - 选择"分辨率"为 **1920x1080**

   - 点击"播放声音"节点
   - 输入"语音文本": **"拍照完成,已拍摄3张照片"**
   - 调整"音量"为 **80%**

4. **保存流程**
   - 点击右上角 **"保存流程"** 按钮
   - 看到提示: "已保存 2 个任务" ✅
   - 点击 **"保存并返回"** 关闭模态框

5. **测试导航**
   - 在地图上点击设置目标点
   - 点击 **"开始导航"**
   - 观察机器人到达后执行: 拍照 → 播放语音

### 4. 创建并行任务流程

#### 场景: 同时拍照和环境扫描

**步骤**:

1. **添加并行节点**
   - 拖拽 ⚡ **"并行执行"** 到画布
   - 连接: 开始 → 并行执行

2. **创建并行分支**
   - 拖拽 📷 **"拍照"** 到并行节点左下方
   - 拖拽 🔍 **"环境扫描"** 到并行节点右下方
   - 连接并行节点的左侧圆点到"拍照"
   - 连接并行节点的右侧圆点到"扫描"

3. **汇合到结束**
   - 分别连接"拍照"和"扫描"到"结束"节点

4. **最终流程**
   ```
        [开始]
           ↓
       [并行执行]
         ↙   ↘
     [拍照]  [扫描]
         ↘   ↙
        [结束]
   ```

5. **保存并测试**
   - 保存流程
   - 导航到目标点
   - 观察拍照和扫描同时执行

### 5. 创建条件分支流程

#### 场景: 检测到目标时拍照,否则继续巡检

**步骤**:

1. **添加检测和条件节点**
   - 拖拽 👁️ **"目标检测"** 到画布
   - 拖拽 🔀 **"条件分支"** 到检测节点下方
   - 连接: 开始 → 检测 → 条件分支

2. **配置检测参数**
   - 点击"目标检测"节点
   - 设置"目标类型": **person**
   - 设置"置信度阈值": **0.7**

3. **配置条件**
   - 点击"条件分支"节点
   - 输入"条件表达式": **detected == true**

4. **创建两个分支**
   - True 分支(左侧绿色圆点):
     - 拖拽 📷 "拍照"
     - 连接条件节点 True → 拍照

   - False 分支(右侧红色圆点):
     - 拖拽 🔊 "播放声音"
     - 连接条件节点 False → 播放声音
     - 设置文本: "未检测到目标"

5. **汇合分支**
   - 连接"拍照" → "结束"
   - 连接"播放声音" → "结束"

6. **最终流程**
   ```
           [开始]
              ↓
         [目标检测]
              ↓
     [条件: detected == true]
          ↙      ↘
       True      False
         ↓          ↓
      [拍照]    [播放提示]
         ↘        ↙
          [结束]
   ```

## 常用操作

### 编辑节点
- **单击节点**: 选中节点 (绿色高亮)
- **直接编辑**: 在节点内的输入框直接修改参数
- **拖动节点**: 点击节点卡片拖动调整位置

### 删除节点/连接
- **选中节点**: 单击节点
- **按 Delete 键**: 删除选中的节点
- **选中连接线**: 单击连接线
- **按 Delete 键**: 删除连接线

### 画布操作
- **缩放**: 鼠标滚轮上下滚动
- **平移**: 按住中键(或 Ctrl + 左键)拖动
- **适应画布**: 点击左下角 "适应画布" 按钮
- **重置视图**: 点击右上角 "重置" 按钮

### 保存和取消
- **保存流程**: 点击 "保存流程" → "保存并返回"
- **取消修改**: 点击 "取消" 或点击模态框外部

## 可用任务节点

### 基础任务 (蓝色/绿色/紫色)
- ⏱️ **等待停留** - 等待指定秒数
- 📷 **拍照** - 拍摄指定数量照片
- 🔄 **执行轨迹** - 执行预定义轨迹

### 感知任务 (青色/粉色)
- 🔍 **环境扫描** - 2D/3D 环境扫描
- 👁️ **目标检测** - 检测特定类型目标

### 交互任务 (橙色/黄色)
- 🔊 **播放声音** - TTS 语音播放
- 📺 **显示信息** - 屏幕显示消息
- 💡 **信号灯** - LED 灯光控制

### 控制流 (红色)
- ⚡ **并行执行** - 多任务同时执行
- 🔀 **条件分支** - 基于条件选择执行路径

## 快捷键

- `Delete` / `Backspace` - 删除选中节点/连接
- `Ctrl/Cmd + A` - 全选
- `Ctrl/Cmd + C/V` - 复制粘贴 (React Flow 内置)
- 滚轮 - 缩放
- 中键拖动 - 平移

## 常见问题

### Q1: 保存时提示"未找到起始节点"?
**A**: 这个问题已修复。现在画布总是包含起始和结束节点。如果仍遇到,请刷新页面。

### Q2: 任务节点没有连接到流程,会保存吗?
**A**: 不会。只有连接到起始节点的任务才会被保存。孤立节点会被忽略。

### Q3: 如何修改已保存的任务流程?
**A**:
1. 点击"配置任务"
2. 切换到"流程图模式"
3. 之前保存的任务会自动显示为流程图
4. 修改后点击"保存流程"

### Q4: 并行任务的执行顺序?
**A**: 并行节点的所有分支会同时开始执行,没有先后顺序。所有分支完成后才会继续后续任务。

### Q5: 条件表达式支持哪些操作?
**A**: 当前支持简单的比较表达式,如:
- `detected == true`
- `battery > 20`
- `confidence > 0.8`

更复杂的表达式需要 ROS 端支持。

## 下一步

### 学习更多
- 📖 阅读 [VISUAL_TASK_FLOW_EDITOR.md](./VISUAL_TASK_FLOW_EDITOR.md) 了解完整功能
- 📖 阅读 [TASK_SYSTEM.md](./TASK_SYSTEM.md) 了解任务系统架构
- 📖 查看 [TASK_USAGE_GUIDE.md](./TASK_USAGE_GUIDE.md) 获取更多示例

### 进阶功能
- 创建复杂的嵌套并行/条件流程
- 使用自定义任务类型
- 集成真实 ROS 环境测试

### 反馈问题
- GitHub Issues: [项目地址]
- 文档问题: 查看 BUGFIX_FLOW_EDITOR.md

---

**祝使用愉快!** 🎉
