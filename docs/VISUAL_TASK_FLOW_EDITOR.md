# 可视化任务流程编辑器 (Visual Task Flow Editor)

## 概述

基于 React Flow 的 Scratch 风格可视化任务编辑器,支持拖拽式任务配置、节点连接、并行执行和条件分支。

## 功能特性

### 1. 双模式支持

**列表模式** (原有):
- 传统的表单式任务配置
- 适合快速配置简单顺序任务
- 支持拖拽排序

**流程图模式** (新增):
- 可视化节点式任务编排
- 支持复杂的并行和条件逻辑
- 类 Scratch 的拖拽交互

切换方式: 在任务配置模态框顶部使用 Segmented 控件切换

### 2. 任务节点类型

#### 基础任务节点
- **等待停留** (⏱️) - 等待指定时间
- **拍照** (📷) - 拍摄照片
- **执行轨迹** (🔄) - 执行预定义轨迹

#### 感知任务节点
- **环境扫描** (🔍) - 2D/3D 扫描
- **目标检测** (👁️) - 检测特定目标

#### 交互任务节点
- **播放声音** (🔊) - TTS 语音播放
- **显示信息** (📺) - 屏幕显示消息
- **信号灯** (💡) - LED 信号控制

#### 控制流节点
- **并行执行** (⚡) - 多任务同时执行
- **条件分支** (🔀) - 基于条件选择分支

### 3. 核心交互

#### 添加任务
1. 从左侧工具箱拖拽任务卡片
2. 拖拽到右侧画布释放
3. 任务节点自动创建

#### 连接任务
1. 点击节点下方的输出端点 (圆点)
2. 拖拽到目标节点的输入端点
3. 创建任务执行顺序连接

#### 编辑任务参数
- 直接在节点卡片内编辑参数
- 修改立即生效,无需额外保存

#### 删除节点/连接
- 选中节点或连接线
- 按 Delete 键或 Backspace 键删除

#### 移动节点
- 拖拽节点卡片可自由移动
- 连接线自动跟随调整

#### 缩放和平移
- 鼠标滚轮: 缩放画布
- 中键拖拽: 平移画布
- 右下角 MiniMap: 全局导航

### 4. 特殊节点使用

#### 并行执行节点
```
创建并行任务流程:
1. 拖入"并行执行"节点
2. 该节点有2个输出端点 (branch-1, branch-2)
3. 分别连接到不同的任务分支
4. 所有分支将同时执行

示例流程:
  [开始]
     ↓
  [并行执行]
    ↙   ↘
 [拍照] [播放声音]
    ↘   ↙
   [结束]
```

#### 条件分支节点
```
创建条件逻辑:
1. 拖入"条件分支"节点
2. 输入条件表达式 (例如: battery > 20)
3. 该节点有2个输出端点:
   - True (绿色): 条件满足时执行
   - False (红色): 条件不满足时执行
4. 分别连接对应的任务流

示例流程:
      [开始]
         ↓
    [检测目标]
         ↓
   [条件: detected]
      ↙   ↘
  True   False
    ↓       ↓
 [拍照]  [播放提示音]
    ↘     ↙
    [结束]
```

## 使用示例

### 示例 1: 简单顺序任务

```
[开始]
  ↓
[等待 5秒]
  ↓
[拍照 1张]
  ↓
[播放声音 "拍照完成"]
  ↓
[结束]
```

操作步骤:
1. 拖入"等待停留"节点,设置 5 秒
2. 拖入"拍照"节点,设置 1 张照片
3. 拖入"播放声音"节点,输入"拍照完成"
4. 按顺序连接: 开始 → 等待 → 拍照 → 声音 → 结束
5. 点击"保存流程"

### 示例 2: 并行任务

```
     [开始]
        ↓
   [到达目标点]
        ↓
    [并行执行]
      ↙   ↘
  [拍照]  [环境扫描]
      ↘   ↙
     [结束]
```

操作步骤:
1. 拖入"并行执行"节点
2. 拖入"拍照"节点,连接到 branch-1
3. 拖入"环境扫描"节点,连接到 branch-2
4. 保存后,机器人会同时执行拍照和扫描

### 示例 3: 条件分支

```
        [开始]
           ↓
      [目标检测]
           ↓
  [条件: confidence > 0.8]
        ↙     ↘
     True     False
       ↓         ↓
   [拍照]   [重新扫描]
       ↓         ↓
  [播放成功]  [播放失败]
       ↘       ↙
        [结束]
```

操作步骤:
1. 拖入"目标检测"节点,设置目标类型
2. 拖入"条件分支"节点,输入 `confidence > 0.8`
3. True 分支: 连接"拍照" → "播放成功"
4. False 分支: 连接"重新扫描" → "播放失败"
5. 两个分支最终汇合到结束节点

### 示例 4: 复杂巡检流程

```
             [开始]
                ↓
          [环境扫描 3D]
                ↓
         [条件: 检测到异常]
            ↙      ↘
         True      False
           ↓          ↓
      [并行执行]   [继续巡检]
        ↙    ↘        ↓
    [拍照]  [录音]  [结束]
        ↘    ↙
      [发送警报]
           ↓
        [等待5秒]
           ↓
        [结束]
```

## 数据转换

### Flow → Tasks 转换规则

1. **深度优先遍历**: 从"开始"节点开始,沿着连接线深度优先遍历
2. **并行节点**: 识别并行节点的多个输出,转换为 `PARALLEL` 任务类型
3. **条件节点**: 识别 True/False 两个分支,转换为 `CONDITIONAL` 任务类型
4. **顺序节点**: 按连接顺序转换为任务数组

### Tasks → Flow 转换规则

1. **顺序任务**: 竖向排列,从上到下连接
2. **并行任务**: 创建并行节点,分支横向展开
3. **条件任务**: 创建条件节点,True 左,False 右
4. **起始/结束**: 自动添加起始和结束节点

## 快捷键

- `Delete` / `Backspace`: 删除选中的节点或连接
- `Ctrl/Cmd + Z`: 撤销 (尚未实现)
- `Ctrl/Cmd + Y`: 重做 (尚未实现)
- `Ctrl/Cmd + A`: 全选 (React Flow 内置)
- `Ctrl/Cmd + C/V`: 复制粘贴 (React Flow 内置)

## 工具栏功能

### 右上角按钮
- **重置**: 恢复到初始任务配置
- **保存流程**: 将流程图转换为任务列表并保存

### 左下角 Controls
- **+ / -**: 缩放控制
- **适应画布**: 自动调整缩放以显示所有节点
- **锁定**: 锁定画布禁止编辑

### 右下角 MiniMap
- 显示整体流程图缩略图
- 当前视口高亮显示
- 点击跳转到对应区域

## 技术实现

### 核心依赖
- `reactflow` - 流程图引擎
- `@ant-design` - UI 组件
- 自定义节点组件 (nodes/)
- 转换工具 (utils/converter.ts)

### 文件结构
```
TaskFlowEditor/
├── index.tsx                 # 主编辑器组件
├── TaskFlowEditor.css        # 自定义样式
├── TaskPalette.tsx           # 左侧任务工具箱
├── TaskPalette.css           # 工具箱样式
├── nodes/                    # 节点组件目录
│   ├── WaitTaskNode.tsx      # 等待任务节点
│   ├── PhotoTaskNode.tsx     # 拍照任务节点
│   ├── TrajectoryTaskNode.tsx
│   ├── ScanTaskNode.tsx
│   ├── InspectTaskNode.tsx
│   ├── SoundTaskNode.tsx
│   ├── DisplayTaskNode.tsx
│   ├── SignalTaskNode.tsx
│   ├── ParallelNode.tsx      # 并行节点
│   └── ConditionalNode.tsx   # 条件节点
└── utils/
    └── converter.ts          # Flow ↔ Tasks 转换逻辑
```

### 自定义节点结构
```typescript
export const WaitTaskNode: React.FC<NodeProps> = ({ data }) => {
  const [duration, setDuration] = useState(data.duration || 5);
  const [label, setLabel] = useState(data.label || '等待');

  return (
    <div className="custom-task-node">
      <Handle type="target" position={Position.Top} />

      <Card style={{ backgroundColor: '#e3f2fd' }}>
        {/* 节点内容 */}
      </Card>

      <Handle type="source" position={Position.Bottom} />
    </div>
  );
};
```

## 性能优化

1. **节点懒加载**: 节点组件按需导入
2. **状态局部化**: 节点参数修改只影响节点自身
3. **连接优化**: 使用 React Flow 内置的连接优化
4. **渲染优化**: 利用 React.memo 避免不必要的重渲染

## 限制与已知问题

1. **循环检测**: 当前不支持循环流程,需要避免创建循环连接
2. **撤销重做**: 尚未实现完整的撤销重做功能
3. **节点验证**: 保存时不会验证流程合理性 (如孤立节点)
4. **导出导入**: 暂不支持流程图的独立导出/导入

## 未来改进方向

1. **历史记录**: 实现完整的撤销/重做栈
2. **流程验证**: 保存前检查流程完整性和合理性
3. **模板库**: 预定义常用任务流程模板
4. **节点分组**: 支持将多个节点组合成子流程
5. **注释节点**: 添加纯注释节点用于说明
6. **实时预览**: 模拟执行流程并显示预期结果
7. **性能分析**: 显示任务流程的预计执行时间

## 参考资料

- [React Flow 官方文档](https://reactflow.dev/)
- [TASK_SYSTEM.md](./TASK_SYSTEM.md) - 任务系统架构
- [VISUAL_TASK_EDITOR_RESEARCH.md](./VISUAL_TASK_EDITOR_RESEARCH.md) - 技术选型研究
