# 任务配置界面使用指南

## 新的交互流程

为了提供更好的用户体验，任务配置现在独立到了专门的配置界面。

### 界面分离设计

#### 1. 导航控制界面（主界面）
**位置**: Navigation组件的侧边栏

**功能**:
- 设置机器人初始位置
- 设置导航目标点
- 查看已配置的任务列表（只读）
- 开始/停止导航
- 查看导航状态和进度

**附加任务卡片**:
```
┌─────────────────────────────────┐
│ 附加任务                         │
├─────────────────────────────────┤
│ 已配置 3 个任务    [配置任务]   │
│                                  │
│ ┌─────────────────────────────┐ │
│ │ ⏱️ 1  等待停留      5秒     │ │
│ │ 📷 2  拍照          3张     │ │
│ │ 🔊 3  播放声音     "完成"   │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

#### 2. 任务配置界面（模态框）
**触发**: 点击"配置任务"按钮

**功能**:
- 添加新任务
- 编辑任务参数
- 删除任务
- 调整任务顺序
- 保存并返回

**界面布局**:
```
┌──────────────────────────────────────────────────────────┐
│ 附加任务配置                               [取消] [保存]  │
├──────────────────────────────────────────────────────────┤
│                                                           │
│ 在此页面配置机器人到达目标点后需要执行的附加任务...      │
│                                                           │
│ ┌─ 添加任务 ─────────────────────────────────────────┐  │
│ │ ▶ 基础任务                                          │  │
│ │   [⏱️ 等待停留] [📷 拍照] [🔄 执行轨迹]           │  │
│ │ ▶ 感知任务                                          │  │
│ │   [🔍 环境扫描] [👁️ 目标检测]                     │  │
│ │ ▶ 交互任务                                          │  │
│ │   [🔊 播放声音] [📺 显示信息] [💡 信号灯]         │  │
│ └──────────────────────────────────────────────────────┘  │
│                                                           │
│ ┌─ 任务列表 (3) ──────────────────────────────────────┐  │
│ │ ┌─ 任务 1: 等待停留 ─────────────────── [编辑] [×] ┐ │  │
│ │ │ 等待时长: 5秒                                     │ │  │
│ │ └──────────────────────────────────────────────────┘ │  │
│ │ ┌─ 任务 2: 拍照 ─────────────────────── [编辑] [×] ┐ │  │
│ │ │ 拍照数量: 3张, 间隔: 1秒                         │ │  │
│ │ └──────────────────────────────────────────────────┘ │  │
│ │ ┌─ 任务 3: 播放声音 ──────────────────── [编辑] [×] ┐ │  │
│ │ │ TTS文本: "任务完成"                              │ │  │
│ │ └──────────────────────────────────────────────────┘ │  │
│ └──────────────────────────────────────────────────────┘  │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## 使用步骤

### 步骤 1: 打开任务配置界面

1. 在导航控制面板中，找到"附加任务"卡片
2. 点击"配置任务"按钮
3. 任务配置模态框弹出

### 步骤 2: 添加任务

1. 在"添加任务"区域，展开任务分类（如"基础任务"）
2. 点击需要的任务类型按钮（如"📷 拍照"）
3. 任务自动添加到列表并进入编辑状态

### 步骤 3: 编辑任务参数

1. 在弹出的编辑表单中配置参数
   - 任务名称（可选）
   - 任务参数（根据类型不同）
   - 超时时间（可选）
2. 点击"保存"按钮确认
3. 或点击"取消"放弃修改

### 步骤 4: 管理任务列表

**编辑任务**:
- 点击任务卡片右上角的"编辑"图标
- 修改参数后保存

**删除任务**:
- 点击任务卡片右上角的"×"图标
- 任务立即从列表移除

**查看任务摘要**:
- 非编辑状态下，任务卡片显示参数摘要
- 例如："等待 5 秒"、"拍照 3 张"

### 步骤 5: 保存并返回

1. 确认任务配置无误后，点击右上角"保存并返回"按钮
2. 模态框关闭，返回导航界面
3. 导航界面的任务列表更新，显示已配置的任务
4. 系统提示"已保存 X 个任务"

### 步骤 6: 开始导航

1. 在导航界面设置机器人初始位置（手动重定位）
2. 设置目标点
3. 点击"开始导航"
4. 机器人到达目标点后，自动按顺序执行配置的任务

## 交互细节

### 1. 任务列表显示（导航界面）

**空状态**:
```
┌─────────────────────────────────┐
│ 暂无任务         [配置任务]     │
│                                  │
│         📋                       │
│   点击上方按钮配置任务           │
└─────────────────────────────────┘
```

**有任务状态**:
```
┌─────────────────────────────────┐
│ 已配置 3 个任务   [配置任务]    │
│ ┌─────────────────────────────┐ │
│ │ ⏱️ 1  等待停留      5秒     │ │
│ │ 📷 2  拍照          3张     │ │
│ │ 🔊 3  播放声音     "完成"   │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

**特点**:
- 只读显示，不可编辑
- 显示任务图标、序号、名称和关键参数
- 最多显示5个任务，超出可滚动查看
- 紧凑布局，不占用过多空间

### 2. 任务配置模态框

**尺寸**:
- 宽度: 90%屏幕宽度，最大1200px
- 高度: 自适应内容，最大calc(100vh - 200px)

**特点**:
- 全屏级别的配置空间
- 内容可滚动，适应大量任务
- 顶部固定操作按钮（取消/保存）
- 关闭模态框需确认（取消按钮）

### 3. 数据持久化

**保存时机**:
- 点击"保存并返回"按钮时保存
- 点击"取消"按钮时放弃更改，恢复原始任务列表

**数据流**:
```
NavigationControl (tasks state)
        ↓ 打开模态框
TaskConfigurationModal (local tasks state)
        ↓ 编辑任务
TaskConfigPanel (编辑组件)
        ↓ 保存
NavigationControl (tasks state 更新)
```

## 优势对比

### 之前的设计（内联编辑）

❌ **缺点**:
- 导航界面过于拥挤
- 任务配置占用大量空间
- 编辑时其他控制被遮挡
- 滚动体验差

### 现在的设计（独立界面）

✅ **优点**:
- 导航界面简洁清爽
- 任务配置有充足空间
- 职责分离，交互清晰
- 保存/取消机制明确
- 可以专注于任务配置，不受导航界面干扰

## 使用场景示例

### 场景1: 快速巡检任务

**需求**: 到达巡检点后拍照并等待

**操作流程**:
1. 点击"配置任务"
2. 添加"拍照"任务，设置3张连拍
3. 添加"等待"任务，设置5秒
4. 点击"保存并返回"
5. 设置目标点，开始导航

**时间**: < 30秒

### 场景2: 复杂检测任务

**需求**: 环境扫描 → 目标检测 → 语音提示 → 信号灯

**操作流程**:
1. 点击"配置任务"
2. 依次添加4个任务：
   - 3D扫描（5秒）
   - 目标检测（person）
   - 播放声音（"检测完成"）
   - 信号灯（绿色闪烁）
3. 逐个编辑参数
4. 点击"保存并返回"
5. 开始导航

**时间**: < 2分钟

### 场景3: 调整现有任务

**需求**: 修改拍照数量从3张改为5张

**操作流程**:
1. 点击"配置任务"
2. 找到"拍照"任务，点击"编辑"
3. 修改数量为5
4. 点击"保存"
5. 点击"保存并返回"

**时间**: < 15秒

## 键盘快捷键（规划中）

- `Ctrl + T`: 打开任务配置
- `Esc`: 关闭模态框（等同于取消）
- `Ctrl + S`: 保存并返回
- `Ctrl + N`: 添加新任务

## 移动端适配

在移动设备上：
- 模态框宽度: 95%
- 按钮尺寸: 增大到48px
- 任务列表项: 增加padding
- 支持触摸滚动

## 常见问题

**Q: 点击"配置任务"后可以直接关闭模态框吗？**
A: 可以，点击"取消"按钮或点击模态框外部区域。未保存的修改会丢失。

**Q: 如果配置了很多任务，导航界面会显示所有任务吗？**
A: 任务列表最多显示约10个任务，超出部分可以滚动查看。建议控制任务数量在10个以内。

**Q: 任务配置会自动保存吗？**
A: 不会。必须点击"保存并返回"按钮才会保存。这样可以避免误操作。

**Q: 可以在导航过程中修改任务吗？**
A: 不可以。任务在导航开始时确定，运行中无法修改。需要停止导航后重新配置。

**Q: 任务配置会在刷新页面后保留吗？**
A: 当前版本不会保留。刷新页面后需要重新配置。后续版本会添加localStorage持久化。

## 未来改进

1. **任务模板**: 保存常用任务组合为模板
2. **快速配置**: 在任务列表中直接快速编辑
3. **任务导入/导出**: JSON格式导入导出任务配置
4. **历史记录**: 查看和恢复之前的任务配置
5. **任务预览**: 在配置界面模拟任务执行流程

## 技术实现

### 组件结构

```
NavigationControl
  └── TaskListView (简化列表)
      ├── 任务图标 + 名称 + 摘要
      └── "配置任务"按钮
  └── TaskConfigurationModal (配置模态框)
      ├── TaskConfigPanel (完整配置面板)
      │   ├── 任务分类选择器
      │   ├── 任务列表
      │   └── TaskEditor (参数编辑器)
      └── 保存/取消按钮
```

### 状态管理

```typescript
// NavigationControl.tsx
const [tasks, setTasks] = useState<TaskConfig[]>([]);
const [taskConfigModalVisible, setTaskConfigModalVisible] = useState(false);

// 保存任务
const handleSaveTasks = (newTasks: TaskConfig[]) => {
  setTasks(newTasks);
  setTaskConfigModalVisible(false);
  message.success(`已保存 ${newTasks.length} 个任务`);
};
```

## 总结

新的任务配置流程提供了：

✅ **更清晰的界面**: 导航和配置分离
✅ **更好的体验**: 充足的配置空间
✅ **更明确的交互**: 保存/取消机制
✅ **更简洁的导航界面**: 只显示关键信息

这样的设计让用户可以专注于当前的操作，无论是配置任务还是执行导航，都有最佳的用户体验。
